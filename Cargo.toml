[workspace]
members = ["daemon"]
resolver = "2"

[workspace.package]
edition = "2024"
rust-version = "1.85.0"

[profile.release]
opt-level = "z"
codegen-units = 1
lto = true
panic = "abort"
strip = true

[profile.release-wasm]
inherits = "release"
opt-level = "z"
lto = true
strip = "symbols"

