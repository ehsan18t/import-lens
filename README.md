# ImportLens

ImportLens is a VS Code extension that displays npm import size estimates inline while editing JavaScript and TypeScript files.

The default display mode uses end-of-line decorations. Users relying on screen readers should set `importLens.display` to `inlayHint`, because VS Code exposes inlay hints through the document model while decorations are visual-only.

