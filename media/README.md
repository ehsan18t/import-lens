# Media Assets

Place the marketplace icon at `media/icon.png` before packaging a release VSIX.
The manifest already points to that path so the final icon can be added without
changing extension metadata.
